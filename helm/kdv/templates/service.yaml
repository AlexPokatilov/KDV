apiVersion: v1
kind: Service
metadata:
  name: {{ include "kdv.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kdv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kdv.selectorLabels" . | nindent 4 }}
