services:
  kdv:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:8000"
    volumes:
      - /opt/kube:/root/.kube:ro
    environment:
      KUBECONFIG: /root/.kube/config
      IN_CLUSTER: "false"
      LOG_LEVEL: info
    restart: unless-stopped
